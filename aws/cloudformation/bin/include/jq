#!/bin/sh -e

#----------------------------------------------
# jq_format
#----------------------------------------------
jq_format() {
  local JQ_RESULT=$(cat)
  [ "[${JQ_RESULT}]" = "[]" ] && echo "null" && return 0
  echo ${JQ_RESULT}
  return 0
}
